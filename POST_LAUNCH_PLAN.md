# خطة ما بعد الإطلاق والمراقبة المكثفة (Post-Launch Hypercare Monitoring Plan)
**المنصة:** RussiaBooking  
**الفترة المغطاة:** أول 14 يوماً من إطلاق الإنتاج (Hypercare Window)  
**فريق العمل:** DevOps, Security, Backend, QA, Payment Support, On-call Engineers  

---

## 1. جدول المناوبات والمناوبة الفورية (24/7 On-Call Rotation)

- **فترة المناوبة (Shifts):** 3 فترات يومياً (8 ساعات لكل مهندس) مع تغطية كاملة لجميع أيام الأسبوع.
- **قنوات التصعيد الآلي:**
  - PagerDuty + Slack `#alerts-production` + SMS مباشر للمهندس المناوب عند التنبيهات الحرجة.
  - وقت الاستجابة الأقصى (MTTA): أقل من **5 دقائق** للتنبيهات الحرجة (P1)، وأقل من **15 دقيقة** للتنبيهات العالية (P2).

---

## 2. لوحات المراقبة والمؤشرات التشغيلية المستهدفة (SLOs & Metrics)

| المؤشر (KPI) | الحد المستهدف (Target) | قناة المراقبة | الإجراء عند التجاوز |
| :--- | :--- | :--- | :--- |
| **نسبة نجاح عمليات الدفع** | > 95% لبطاقات مدى وفيزا | Sentry + Datadog | إشعار فوري لمهندس المدفوعات والتحقق من اتصال بوابات Tap / Mada. |
| **زمن استجابة الـ API (p95)** | < 200 ms | Prometheus / Grafana | التوسع الأفقي التلقائي للحاويات (Auto-scaling) وزيادة عدد الـ Replicas. |
| **معدل الأخطاء (HTTP 5xx)** | < 0.05% | Cloud Run Metrics | فحص سجلات الأخطاء في Sentry و Rollback فوري إذا ارتبطت بنشر جديد. |
| **حالات الحجز المزدوج (Overbooking)** | **0% تماماً** | Postgres Audit Ledger | إيقاف الغرفة مؤقتاً ومراجعة قفل التزامن (Redis Lock). |

---

## 3. كتيب الإجراءات السريعة لحالات الطوارئ (Emergency Runbooks)

### سيناريو 1: فشل متكرر في بوابة الدفع (Payment Gateway Degradation)
1. الدخول على لوحة الإدارة وتفعيل التبديل التلقائي لمسار الدفع البديل (Fallback Gateway Routing).
2. إشعار المستخدم برسالة واضحة لاختيار طريقة دفع بديلة (مثل: بطاقة ائتمانية أخرى أو تمارا).
3. التواصل مع فريق الدعم الفني لبوابة الدفع للتحقق من سلامة خوادمهم أو شبكة التحويل البنكي السعودي.

### سيناريو 2: هجوم حجب خدمة أو محاولات تخمين (Brute-force / DDoS Attack)
1. تفعيل وضع الحماية الصارمة (Under Attack Mode) في Cloudflare أو AWS WAF بنقرة واحدة.
2. عزل وحجب نطاقات الـ IP المشبوهة تلقائياً عبر نظام كشف التسلل (WAF Rate Limiting).
3. تدقيق ترويسات الطلبات والتأكد من عدم وصول أي حمل غير مصرح به لخوادم التطبيق الداخلية.

### سيناريو 3: الحاجة إلى التراجع الفوري (Emergency Rollback)
1. تشغيل أمر التراجع السريع إلى النسخة المستقرة السابقة:
   ```bash
   gcloud run services update-traffic russiabooking-prod --to-revisions=PREVIOUS_HEALTHY_REVISION=100
   ```
2. التأكد من عودة الاستقرار خلال أقل من دقيقة واحدة.
