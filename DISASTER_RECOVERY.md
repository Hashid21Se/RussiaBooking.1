# خطة استمرارية الأعمال والتعافي من الكوارث (Disaster Recovery & Business Continuity Plan)
**المنصة:** RussiaBooking  
**مستوى الخدمة المستهدف (SLA):** 99.95% توفر سنوي  
**تاريخ المراجعة:** سبتمبر 2026  

---

## 1. الأهداف الزمنية للتعافي (RTO & RPO Targets)

| المقياس | الهدف المعتمد (Target) | الآلية التقنية لتحقيق الهدف |
| :--- | :--- | :--- |
| **RPO (Recovery Point Objective)**<br>أقصى فترة مقبولة لفقدان البيانات | **< 5 دقائق** | • النسخ الاحتياطي اللحظي المستمر (Continuous WAL Archiving) عبر Aurora PostgreSQL.<br>• الاستعادة الدقيقة لأي نقطة زمنية سابقة (Point-in-Time Recovery - PITR).<br>• تسجيل كافة المعاملات المالية فوراً في دفتر الأستاذ المشفر (Audit Ledger). |
| **RTO (Recovery Time Objective)**<br>أقصى وقت لإعادة تشغيل الخدمة بعد كارثة كبرى | **< 15 دقيقة** | • نشر حاويات جاهزة بنظام Multi-AZ مع توجيه تلقائي للحركة.<br>• بنية تحتية كودية بالكامل (Terraform) تمكن من إعادة إنشاء العنقود السحابي بالكامل تلقائياً.<br>• فحوصات صحية تلقائية تفصل الحاويات المتعطلة وتحل مكانها حاويات سليمة في ثوانٍ. |

---

## 2. مستويات الكوارث وسيناريوهات الاستجابة (Tiered Disaster Scenarios)

### المستوى الأول: تعطل خادم أو حاوية فردية (Container / Pod Failure)
- **الاحتمال:** مرتفع
- **الإجراء التلقائي:** يقوم المنسق (Kubernetes / Cloud Run) بإنهاء الحاوية المتضررة وبدء حاوية جديدة فوراً من الصورة الجاهزة (`ghcr.io/russiabooking:latest`).
- **تأثير المستخدم:** منعدم (Zero Downtime) بفضل وجود موازن الأحمال (ALB).

### المستوى الثاني: عطل في قاعدة البيانات الرئيسية (Primary Database Failure)
- **الاحتمال:** متوسط
- **الإجراء التلقائي:** يتم تفعيل الفشل التلقائي (Automatic Multi-AZ Failover) إلى النسخة الاحتياطية المتزامنة في المنطقة الجغرافية الثانية خلال **30 إلى 60 ثانية** دون أي تدخل يدوي ودون فقدان لأي معاملة.

### المستوى الثالث: خروج منطقة سحابية كاملة عن الخدمة (Cloud Region Outage)
- **الاحتمال:** نادر
- **الإجراء:**
  1. تفعيل سياسة توجيه DNS (Cloudflare / Route 53 Health Checks Failover) لتحويل كل الحركة إلى المنطقة الجغرافية البديلة (Secondary Region - Frankfurt/Frankfurt EU or Riyadh).
  2. تشغيل سكريبت الاستعادة الآلي:
     ```bash
     ./scripts/restore-db.sh /backups/latest-snapshot.sql.gz russiabooking
     ```
  3. استعادة الاتصال ببوابات الدفع وإجراء فحص تكامل المعاملات (Reconciliation).

---

## 3. جدول الاختبارات الدورية لمحاكاة الكوارث (DR Drills)
- **اختبار استعادة البيانات الشهري:** سكريبت مؤتمت في أول كل شهر ميلادي يقوم بإنشاء قاعدة بيانات مؤقتة واستعادة أحدث نسخة احتياطية ومطابقة عدد السجلات والتحقق من سلامة التشفير (`AES-256-GCM`).
- **فحص التسامح مع الأخطاء (Chaos Engineering):** محاكاة فصل مفاجئ لاتصال Redis أو إنهاء حاوية أثناء تدفق حجز للتأكد من سلوك التراجع (Rollback) وعدم سحب أموال العميل دون إصدار الحجز.
